analyticsCharts.line({
    container: "${container}",
    width : ${width},
    height : ${height},
    labels : ${labels},
    xlen : ${xlen},
    xdata : ${xdata},
    ydata : ${ydata},
    colors: ${colors},
    xsteps : ${xsteps},
    breaks : ${breaks},
    ylabel : "${ylabel}"
});
