analyticsCharts.performanceLine({
    container: "${container}",
    width : ${width},
    height : ${height},
    labels : ${labels},
    xlen : ${xlen},
    xdata : ${xdata},
    timeData : ${timeData},
    thruData : ${thruData},
    colors: [],
    xsteps : ${xsteps},
    breaks : ${breaks}
});
